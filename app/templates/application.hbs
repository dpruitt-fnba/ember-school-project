<h1>Chef Tracker</h1>
<ul>
{{#each chefs as |chef|}}
  {{#if chef.isCookingToday}}
    <li class="available">{{chef.name}}</li>
  {{else}}
    <li class="unavailable">{{chef.name}}</li>
  {{/if}}
{{/each}}
</ul>