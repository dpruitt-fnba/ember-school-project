<div class="form-group">
  <label>Name</label>
  {{input value=chef.name}}
</div>

<div class="form-group">
  <label>Description</label>
  {{textarea value=chef.description}}
</div>

<button {{action 'save'}}>Save</button>
<button {{action "fireChef" chef}}>Fire</button>
